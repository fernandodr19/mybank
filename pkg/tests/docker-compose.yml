version: '3'

services:
  pg-test:
    container_name: pg-tx-test
    image: postgres:13.2
    ports:
      - 5436:5432
    environment:
      POSTGRES_DB: test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres